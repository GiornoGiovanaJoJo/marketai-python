# 🛠️ Скрипты для миграции Frontend

## migrate_frontend.py

Автоматический перенос всего frontend кода из `marketai-front` в `marketai-python/frontend`.

### 📝 Предварительные требования

1. **Python 3.8+** установлен
2. **Git** для работы с репозиториями
3. **Node.js & npm** для установки зависимостей

### 🚀 Быстрый старт

```bash
# 1. Клонировать оба репозитория в одну директорию
cd ~
mkdir marketai-migration
cd marketai-migration

# Клонировать исходный репозиторий
git clone https://github.com/GiornoGiovanaJoJo/marketai-front.git

# Клонировать целевой репозиторий
git clone https://github.com/GiornoGiovanaJoJo/marketai-python.git

# 2. Перейти в целевой репозиторий
cd marketai-python

# 3. Переключиться на ветку миграции
git checkout feature/full-frontend-migration

# 4. Запустить скрипт миграции
python scripts/migrate_frontend.py
```

### 📋 Что делает скрипт

Скрипт автоматически:

1. ✅ Проверяет наличие исходного репозитория `marketai-front`
2. ✅ Создает резервную копию существующих файлов (`frontend_backup/`)
3. ✅ Копирует корневые файлы:
   - `src/App.tsx`
   - `src/main.tsx`
   - `src/index.css`
   - `src/vite-env.d.ts`

4. ✅ Копирует все директории:
   - `src/components/` (~60 компонентов)
   - `src/contexts/` (AuthContext, ThemeContext)
   - `src/hooks/` (custom hooks)
   - `src/lib/` (утилиты)
   - `src/pages/` (26 страниц)
   - `src/services/` (API сервисы)
   - `src/store/` (Redux store)
   - `src/types/` (TypeScript типы)

5. ✅ Проверяет `package.json` на недостающие зависимости

### 📄 Структура проекта

Перед запуском:
```
marketai-migration/
├── marketai-front/          # Исходный репозиторий
│   └── src/
│       ├── components/
│       ├── contexts/
│       ├── pages/
│       └── ...
└── marketai-python/        # Целевой репозиторий
    ├── frontend/
    │   └── src/
    │       ├── components/      # Будет перезаписано
    │       └── ...
    └── scripts/
        └── migrate_frontend.py
```

После запуска:
```
marketai-python/
├── frontend/
│   └── src/                   # Полностью мигрировано! ✅
│       ├── App.tsx
│       ├── main.tsx
│       ├── index.css
│       ├── components/        # 60+ компонентов
│       ├── contexts/          # Auth, Theme
│       ├── hooks/
│       ├── lib/
│       ├── pages/             # 26 полных страниц
│       ├── services/
│       ├── store/             # Redux
│       └── types/
└── frontend_backup/          # Резервная копия
```

### ⚠️ Важно

1. **Резервная копия:** Скрипт автоматически создает `frontend_backup/` перед перезаписью файлов

2. **Перезапись:** Все существующие файлы в `frontend/src/` будут перезаписаны

3. **API изменения:** После миграции необходимо обновить API вызовы в `src/services/` для работы с Django вместо Laravel

### 🔍 Проверка после миграции

```bash
# Проверить изменения
git status

# Посмотреть дифф (опционально)
git diff frontend/src/

# Перейти в frontend и установить зависимости
cd frontend
npm install

# Запустить dev сервер
npm run dev

# Открыть браузер на http://localhost:3000
```

### 🐛 Возможные проблемы

#### 1. API эндпоинты не работают

**Причина:** Laravel API и Django API имеют разные эндпоинты

**Решение:**
- Обновите `frontend/src/services/api.ts`
- Перепишите все эндпоинты на Django URL

#### 2. TypeScript ошибки

**Причина:** Несовместимые типы или отсутствующие импорты

**Решение:**
```bash
npm run type-check
```
Исправьте ошибки по одной

#### 3. Redux store не работает

**Причина:** Структура API response изменилась

**Решение:**
- Проверьте Redux DevTools в браузере
- Обновите slices под новую структуру Django

#### 4. Стили не применяются

**Причина:** Не установлены зависимости Tailwind

**Решение:**
```bash
npm install tailwindcss postcss autoprefixer
```

### 📝 Следующие шаги после миграции

1. ✅ **Установить зависимости**
   ```bash
   cd frontend && npm install
   ```

2. 🔧 **Обновить API сервисы**
   - Изменить `baseURL` в `src/services/api.ts`
   - Обновить все эндпоинты для Django

3. 🧪 **Протестировать**
   ```bash
   npm run dev
   ```
   - Открыть http://localhost:3000
   - Проверить все 26 страниц
   - Проверить аутентификацию

4. ✅ **Исправить ошибки**
   ```bash
   npm run type-check  # TypeScript
   npm run lint        # ESLint
   ```

5. 📝 **Закоммитить**
   ```bash
   git add .
   git commit -m "feat: Полная миграция frontend из marketai-front"
   git push origin feature/full-frontend-migration
   ```

6. 🔀 **Создать Pull Request**
   - Перейти на GitHub
   - Создать PR из `feature/full-frontend-migration` в `main`
   - Описать изменения

### 📊 Ожидаемый результат

После запуска скрипта вы получите:

- ✅ **~150 файлов** скопировано
- ✅ **~500 KB кода** перенесено
- ✅ **26 страниц** полностью функциональны
- ✅ **Все компоненты** на месте
- ✅ **Redux store** готов к использованию
- ⚠️ **API сервисы** требуют обновления под Django

### ℹ️ Помощь

Если возникли проблемы:

1. Проверьте логи скрипта
2. Откатите изменения из `frontend_backup/`
3. Откройте issue на GitHub

---

**Создано:** 2025-11-23  
**Автор:** Migration Bot  
**Версия:** 1.0.0
