# Django Settings
DJANGO_SECRET_KEY=django-insecure-test-key-g7k@2!m#n$p%q^r&s*t+u=v-w_x~y|z{a}b[c]d<e>f
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend
DJANGO_CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000

# Field Encryption Key (for encrypting sensitive data like API keys)
# This is a VALID test key for local development ONLY
# For production, generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=6LJ8jK9wYx5vN2pQmR4tU7vA3bC5dE8fG1hI2jK4lM=

# Database
DATABASE_URL=postgresql://marketai:marketai@postgres:5432/marketai
DB_NAME=marketai
DB_USER=marketai
DB_PASSWORD=marketai
DB_HOST=postgres
DB_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0
CACHE_REDIS_URL=redis://redis:6379/1
CELERY_BROKER_URL=redis://redis:6379/2

# RabbitMQ (alternative to Redis for Celery - currently using Redis)
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# JWT Settings
JWT_SECRET_KEY=jwt-insecure-test-key-for-local-development-only-change-in-production
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# Email (optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Wildberries API
WILDBERRIES_API_KEY=
WILDBERRIES_API_URL=https://suppliers-api.wildberries.ru

# DuckDB
DUCKDB_PATH=/app/data/analytics.duckdb

# Celery
CELERY_TASK_ALWAYS_EAGER=False

# Frontend Environment Variables
VITE_API_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws
NODE_ENV=development
